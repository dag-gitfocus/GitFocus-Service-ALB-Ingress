apiVersion: v1
kind: Service
metadata:
  name: "gitfocus-alb-ingress-service"
  namespace: "gitfocus-ns1"
spec:
  ports:
    - port: 4041
      targetPort: 4041
      protocol: TCP
  type: NodePort
  selector:
    app: "gitfocus"