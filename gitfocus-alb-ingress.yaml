apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "gitfocus-alb-ingress-ingress"
  namespace: "gitfocus-ns1"
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
  labels:
    app: gitfocus-alb-ingress-ingress
spec:
  rules:
    - http:
        paths:
          - path: /*
            backend: 
              serviceName: "gitfocus-alb-ingress-service"
              servicePort: 4041